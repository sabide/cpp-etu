# Programmes corrigés — TD/TP 2

- `etape3_fonctions_libres/` : surcharge et signatures `T&` / `const T&` ;
- `etape4_fonctions_membres/` : version finale du TD avec fonctions membres ;
- `diagnostics/` : deux modifications interdites par `const`.

```bash
make
make run
make diagnostics
```

La dernière commande réussit seulement si les deux programmes volontairement
incorrects sont refusés par le compilateur.
