# Programmes corrigés — TD/TP 1

Les fichiers `ex1_...` à `ex6_...` donnent les solutions exécutables. Le
fichier `ex3_reference_return.cpp` correspond au mini-exercice `toto(z)=18`.

```bash
make
make run
```

Les fichiers de `diagnostics/` sont volontairement incorrects. La commande
`make diagnostics` vérifie que le compilateur les refuse.
